# P7.06-UI4 — First real owner interaction closure

Status: `Complete / PASS`
Date: `2026-08-19`
Owner: `ООО «Арвектум»`
Task classification: `platform` with `governance`
Constitution: `1.2.0` (`Ratified`)
Relevant Accepted RFCs: RFC-0001 `1.0.0`, RFC-0003 `1.0.0`, RFC-0005 `1.0.0`, RFC-0006 `1.0.0`
Roadmap task: `P7.06-UI4 — First real owner interaction proof`
Declared environment: `Persistent Internal / owner-operated`
Selected host: Mac mini, private loopback browser surface
Exact canonical/runtime release: `1da5600963dcba982d3b1969480fd3f725133e12`

## 1. Closure result

`P7.06-UI4 = Complete / PASS` for the declared selected-owner private workspace scope.

The owner opened the live Arvectum OS workspace in Safari against the selected persistent runtime, inspected real retained governed state across the required workspace surfaces, inspected one exact-version/provenance/reconstruction chain, and executed one real bounded UI4 preflight. The preflight remained fail-closed because no action-specific Authorization, Organizational Authority, Data Governance or Consequential Approval evidence was supplied.

No canonical mutation, product/external effect, Organizational Authority or consequential approval was created by the browser session, technical access, button state or preflight execution.

This closure also incorporates and closes one material operator-friction defect discovered by the real owner proof: the initial Safari unlock flow failed before secret validation because the private UI combined `Referrer-Policy: no-referrer` with an exact non-null same-origin POST requirement. PR `#73` remediated that browser metadata self-conflict by changing only the response policy to `same-origin` while keeping exact loopback Host, exact Origin and CSRF checks fail-closed.

## 2. Canonical and runtime continuity

At final proof:

- canonical repository: `arvectum/arvectum-os`;
- canonical `main` / selected-Mac checkout HEAD: `1da5600963dcba982d3b1969480fd3f725133e12`;
- active runtime release: `1da5600963dcba982d3b1969480fd3f725133e12`;
- UI3 status: `PASS`;
- private listener: `127.0.0.1:8766`;
- runtime health shown to the owner: `healthy`;
- Organization: `aa4e760c379c8952aba6c6c335f3e233`;
- Actor: `e4fc60984850106dbfc922ba30ec2332` (`human / attributable`).

The exact-release selected-Mac technical verifier independently returned `status = PASS` on the same release.

## 3. Real retained governed state inspected by owner

The owner navigated the live workspace surfaces:

- Records;
- Executions;
- Evidence;
- Documents.

The required real retained governed item was visible as:

- semantic type: `platform.document`;
- Subject: `document-subject/eis-0344100006426000005-exact-attachment-evidence@aa4e760c379c8952aba6c6c335f3e233`;
- exact Version: `document-version/eis-0344100006426000005-74e943d855406b04@aa4e760c379c8952aba6c6c335f3e233`;
- schema: `p7.06-ui1-real-eis-evidence-1`;
- authority mode: `External Reference`;
- authority scope: `platform.document/external-reference`;
- authoritative source: `ЕИС / zakupki.gov.ru`;
- classification: `restricted-pilot`;
- source release: `b1b78ed9772727dda41b2e509675691f978957ec`;
- validation presentation: `CAP-001 admitted; RFC-0006 provenance admitted; CAP-004 reconstruction complete`.

The UI presented surface grouping explicitly as a non-authoritative projection over retained semantic type rather than a canonical-type or lifecycle mutation.

## 4. Exact provenance / reconstruction chain inspected

The owner inspected the UI4 preflight representation of the same retained source and confirmed:

- Execution Subject: `execution-subject/p7-06-ui1-real-state-74e943d855406b04@aa4e760c379c8952aba6c6c335f3e233`;
- Execution Version: `execution-version/p7-06-ui1-real-state-74e943d855406b04-v5@aa4e760c379c8952aba6c6c335f3e233`;
- Admission Event Version: `event-version/p7-06-ui1-document-admitted-74e943d855406b04-v1@aa4e760c379c8952aba6c6c335f3e233`;
- recovery checkpoint: `582ec80686ce5d2bb3b1eb5779c2fdce3b6899379161e9a953c64e4dcf11d5f7`;
- retained storage locator: `2f5bb6cdd12d3a396cf2c94e2151faaf5204351b4d3d6a98edc2ce739d0f40b7`;
- reconstruction status: `CAP-004 reconstruction complete`;
- provenance references included the attributable principal, exact Execution Subject/Version, exact admitted Event Subject/Version, exact P6.02 Product Contract Subject/Version and source-evidence SHA-256 `74e943d855406b04741f040fed271bddfaada9a9cc6e7da4501735a6e8725121`.

This evidence preserves RFC-0006 provenance/reconstruction semantics without treating reconstruction or external-source visibility as Organizational Authority.

## 5. Real bounded preflight result

Before the POST, the owner saw all four RFC-0005/RFC-0003-relevant decision categories independently represented as `Waiting`:

| Gate | Final visible state | Basis |
|---|---|---|
| Authorization | `Waiting` | no action-specific authorization decision; `workspace.interact` remains technical access only |
| Organizational Authority | `Waiting` | UI3/P7.04 supply no Organizational Authority and UI4 introduces no delegation |
| Data Governance | `Waiting` | no purpose-specific data-governance decision supplied for a canonical change |
| Consequential Approval | `Waiting` | browser/session/button state is not approval |

The owner clicked `Run governed preflight` once.

Browser result:

> `Preflight executed: WAITING / fail-closed. No canonical mutation or external effect was requested.`

This is the intended result. UI4 proves real bounded interaction through the live workspace without manufacturing gate evidence to force a successful consequential action.

## 6. Independent selected-Mac technical verifier

The exact-release verifier `reference/python/p7_06_ui4_selected_mac_proof.py` returned `PASS` with these relevant results:

- `browser_preflight_post_observed = true`;
- `technical_interaction_access = true`;
- `organizational_authority_provided = false`;
- `consequential_approval_provided = false`;
- `canonical_mutation_performed = false`;
- `product_or_external_effect_performed = false`;
- all four gate states = `Waiting`;
- exact source Subject/Version, Execution Version, Event Version and checkpoint matched the current retained-state reconstruction;
- owner-local minimized browser-preflight evidence basename: `p7-06-ui4-owner-preflight-last.json`;
- owner-local evidence SHA-256: `63416f1862168d9a464a30d1824198ad52be4439e9f43fba71360c7ac34a9f91`.

The verifier intentionally reports:

- `human_visual_navigation_attested = false`;
- `operator_friction_review_pending = true`.

Those fields are not failures. Automation cannot attest what the owner visually inspected or judge operator friction. This canonical review supplies the independent human closure input from the actual owner session.

No access secret, browser session cookie, CSRF token or reusable credential material is retained in this canonical evidence.

## 7. Safari unlock defect and remediation evidence

### Iteration 1 — real owner defect discovery

Initial real owner unlock on release `ff0817cce2c4b3a1daad7f4cef437c749eac711e` remained on `/ui3/unlock` with no `Access denied.` message.

A secret-safe local HTTP diagnostic proved:

- `GET /ui3/unlock = 200`;
- exact same-origin `POST /ui3/unlock = 303`;
- process-local session cookie issued;
- authenticated workspace `GET / = 200`;
- authenticated `GET /ui4/preflight = 200`.

Disposition: `REVISE` — runtime/session/secret path healthy; browser request metadata boundary defective.

### Iteration 2 — bounded security-preserving remediation

PR `#73` changed only the shared private UI response `Referrer-Policy` from `no-referrer` to `same-origin` while retaining:

- strict exact loopback Host validation;
- strict exact loopback Origin validation;
- rejection of missing, `null`, foreign and wrong-port Origin;
- CSRF continuity;
- owner-local ingress secret;
- process-local HttpOnly/SameSite session;
- exact P7.04 human grants;
- no-authority / no-approval / no-direct-mutation boundaries.

Focused regression coverage was added in `reference/python/tests/test_p7_06_ui3_browser_unlock_compat.py`.

Reference Python CI:

- run `#149` — `success` before review synchronization;
- run `#150` — `success` on final PR head.

PR `#73` merged to `main` as `1da5600963dcba982d3b1969480fd3f725133e12`.

### Iteration 3 — selected-Mac re-proof

The selected Mac was advanced through the existing canonical P7.06 governed update path to exact release `1da5600963dcba982d3b1969480fd3f725133e12`.

Final UI3 status returned `PASS` on `127.0.0.1:8766`.

The owner then opened a newly loaded Safari unlock page, submitted the owner-local secret and successfully entered the live Arvectum OS workspace.

Disposition: `PASS` for the browser-unlock remediation.

## 8. Functional cross-review

### Iteration 1 — owner interaction evidence completeness

Question: does the evidence prove a real owner browser session rather than a repository-only or synthetic path?

Result: yes. The owner supplied direct visual observations from the live workspace, including exact Organization/Actor/runtime context, navigation, exact retained source identity/version/provenance and the rendered preflight result.

Disposition: `PASS`.

### Iteration 2 — authority/security separation

Question: did successful technical unlock, session creation or button activation silently satisfy Authorization, Organizational Authority, Data Governance or Consequential Approval?

Result: no. All four action gate categories remained independently `Waiting`; the technical verifier independently confirms no Organizational Authority, consequential approval, canonical mutation or product/external effect.

Disposition: `PASS`.

### Iteration 3 — provenance / authority-source integrity

Question: did the workspace turn the retained EIS evidence into competing Arvectum OS authority or hide exact-version lineage?

Result: no. The item remained `External Reference`, authoritative source remained `ЕИС / zakupki.gov.ru`, exact Subject/Version and Execution/Event/checkpoint lineage were visible, and CAP-004 reconstruction was presented as evidence rather than authority.

Disposition: `PASS`.

### Iteration 4 — operator friction and remediation closure

Question: is the material Safari unlock defect either unresolved or hidden from closure evidence?

Result: no. The defect was explicitly recorded, root-caused, remediated through PR `#73`, regression-covered, CI-passed, governed-update deployed and successfully re-proved in the same real browser environment. No additional material operator-friction or authority/usability defect was reported in the final required navigation/preflight pass.

Disposition: `PASS`.

No material objection remains after four functional review/revise iterations.

## 9. Closure decision

The minimum canonical UI4 proof is satisfied:

1. live workspace opened by the owner in a real browser — `PASS`;
2. Organization/Actor/runtime health visibly confirmed — `PASS`;
3. Records, Executions, Evidence and Documents navigated over real retained state — `PASS`;
4. exact Subject/Version/provenance/reconstruction chain inspected — `PASS`;
5. one bounded governed preflight executed — `PASS`, result `WAITING / fail-closed`;
6. operator friction recorded and material defect remediated — `PASS`;
7. exact-release technical verifier — `PASS`;
8. functional cross-review — `PASS`, four iterations, no remaining material objection.

Therefore:

`P7.06-UI4 = Complete / PASS`.

The parent `P7.06-UI — Live operator workspace over persistent runtime` may now close at `100% / Complete / PASS` for the declared `Persistent Internal / owner-operated` scope.

## 10. Non-claims

This closure does **not** establish:

- external/customer Production;
- a public Arvectum OS product UI;
- a Stable/public API, route, browser session contract or SDK;
- a supported browser compatibility matrix;
- `Active` Platform Capability status;
- Stable Product Contract status;
- Organizational Authority or consequential approval from technical UI access;
- a canonical mutation merely because a preflight was executed;
- product/external effect;
- final frontend/deployment architecture;
- SLA/SLO/support or broader conformance promotion.

Safari success in this selected owner environment is proof for this bounded closure, not a general browser-support commitment.

## 11. Next canonical action

Per the Phase 7 and master-roadmap sequence, after canonical roadmap synchronization the next action is:

> **P7.07 — Persistent Tender Operator operational contour.**

P7.07 must preserve the applicable Product Contract boundary, external EIS authority, attributable persistent context and normal RFC-0003/RFC-0005 governance; this UI closure grants no ambient product authority.